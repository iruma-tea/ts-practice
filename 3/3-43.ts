{
    // NG. パラメータ a, b は any 型になるためエラー
    function addNumbers(a, b) {
        return a + b;
    }
    // >> Parameter 'a' implicitly has an 'any' type.
    // >> Parameter 'b' implicitly has an 'any' type.

}