{
class Person {
    constructor(name: string, age: number) {
        this.name = name;
        this.age = age;
    }
    // プロパティ 'name' は型 'Person' に存在しません。
    // プロパティ 'age' は型 'Person' に存在しません。
}
}